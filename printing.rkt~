#lang racket





(define (row i n)
  (apply beside (map (thunk* i) (range n))))

#;(define page
    (above
     (row lparen 6)
     (row rparen 6)
     (row the-dot 6)
     (row the-star 6)
     (row lparen 6)
     (row rparen 6)
     (row the-dot 6)
     (row the-star 6)))
